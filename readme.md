### Resources

Everything served by server. browser reads and paints contents
favicon served in url hosted by server for browser to read
css and js served in urls, browser reads them
link rel does not access file path (of course), its actually url path, hosted by server,
